name: react-emoji-search

install:
    - curl -sL https://deb.nodesource.com/setup_8.x | sudo -E bash -
    - sudo apt-get install -y nodejs
    - npm install

information:
    title: react-emoji-search

project:
    active:
        - src/index.js
    run:
        - ((bouncecode open http://localhost:3000/ -p 3000) &) && npm start
